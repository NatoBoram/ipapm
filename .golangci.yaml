version: "2"

formatters:
  enable:
    - gofumpt

linters:
  settings:
    errcheck:
      exclude-functions:
        - (*compress/gzip.Reader).Close
        - (*github.com/ipfs/kubo/client/rpc.Response).Close
        - (*os.File).Close
        - (io.Closer).Close
        - os.Setenv
